NOTAS:
    - Las imágenes tienen cada una una altura distinta. Por ahora, estamos entrenando con un batch_size = 1.
    En caso de que usaramos más de una o se escalan todas a la misma altura o se hace padding para alto y ancho.
    El código está preparado para ambas cosas.

    - kern y bekern resultan idénticos tras el preprocesamiento que se hace del fichero. -> Usamos kern.

    - Comprobar que se enmascara correctamente la salida del encoder (cuando batch_size > 1); es decir, que se obvia el padding.

    - En el Transformer multimodal, ¿debería el color del background ser el mismo para ambos? 